#!/usr/bin/env bash
# Pre-commit hook opsional — aktifkan dengan:
#   git config core.hooksPath .githooks
# Satu pintu pemeriksaan cepat; unit test otomatis bila Java/Gradle tersedia.
set -u

exec python3 scripts/check_repo.py --pre-commit
